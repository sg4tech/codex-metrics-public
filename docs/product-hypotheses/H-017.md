# The product-hypotheses doc should be reorganized around actual decision flow

Single-hypothesis file.

### H-017 — The product-hypotheses doc should be reorganized around actual decision flow

- Status: `active`
- Created: `2026-04-02`
- Statement:
  - The current `docs/product-hypotheses.md` structure is awkward enough that it slows down adding, revisiting, and comparing hypotheses, so the doc should be rewritten to better match how we actually use it.
- Why it matters:
  - If the working surface is clumsy, hypotheses are less likely to be recorded, re-evaluated, and kept current.
- Expected upside:
  - lower friction when logging and revisiting product ideas
  - better maintenance of hypothesis history
  - less time spent searching or re-parsing the doc
- Expected resource cost:
  - low for a first structural pass
  - medium if the rewrite also needs template changes, section reordering, or migration of existing entries
- Main risks or where this may be wrong:
  - the change could be mostly cosmetic and not improve workflow enough to justify the effort
  - over-structuring the doc could make it harder to scan
  - time spent on formatting could crowd out more important decision work
- Alternatives considered:
  - keep the current document and only append new entries
  - split the log into multiple smaller docs
  - move to a more database-like or tool-generated format
- Current confidence:
  - `medium`
- Evidence status:
  - current usage is frustrating enough to raise the issue now, but I have not measured the time saved by a rewrite yet
- Evaluation plan:
  - revise the doc structure and then compare how quickly a new hypothesis can be added and updated
  - check whether the new layout reduces adjacent edits and makes re-evaluation easier
- Next re-evaluation trigger:
  - after the first reworked pass is used for at least a couple of real hypothesis updates
- Notes:
  - `2026-04-02`: implemented the one-hypothesis-per-file layout so future edits can stay local and context-light.
  - `2026-04-02`: added after the user explicitly noted that the current product-hypothesis documentation is uncomfortable to work with.
  - `2026-04-02`: first-pass refactor landed with a quick index and delta-first writing rules to reduce future reading and editing cost.
