# Planned Hypotheses

This directory is reserved for hypotheses that have been selected for near-term work but have not started yet.

Use it for items that are already real enough to schedule, but not active enough to be in the main working set.

- Keep one hypothesis per file.
- Move a file into `active/` once work starts.
- Move a file into `archive/` once the hypothesis is closed.
