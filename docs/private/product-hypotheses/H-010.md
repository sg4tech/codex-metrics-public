# Agent-agnostic tracking may create more product leverage than staying Codex-only

Single-hypothesis file.

### H-010 — Agent-agnostic tracking may create more product leverage than staying Codex-only

- Status: `active`
- Created: `2026-04-02`
- Statement:
  - Extending `codex-metrics` from a Codex-only tracking workflow to an agent-agnostic workflow may increase the product's usefulness more than continuing to optimize only the Codex-specific path, as long as the public CLI contract stays universal across agents.
- Why it matters:
  - If true, the product should evolve toward comparing workflow quality, retry pressure, and economics across multiple agent ecosystems instead of assuming one agent is the only important operating surface.
- Expected upside:
  - increase the number of real repositories and workflows where `codex-metrics` can be used without forcing a Codex-only operating model
  - allow later comparison of outcome quality and retry pressure between Codex and Claude work instead of keeping that difference hidden in notes
  - reduce product lock-in around one telemetry source and make the analysis layer more durable if teams mix agents
  - create a clearer path for future cross-agent cost and quality benchmarking
- Expected resource cost:
  - low-to-medium for the first step, because agent labeling and instruction-file bootstrap support are additive and backward-compatible
  - medium-to-high for full value realization, because reliable Claude usage backfill may require a separate telemetry backend with its own discovery, tests, and maintenance burden
- Main risks or where this may be wrong:
  - the product may become more generic in wording without gaining much real decision value if most serious usage remains Codex-only
  - adding agent labels without reliable non-Codex auto-usage ingestion may create a partially supported workflow that looks more complete than it is
  - cross-agent comparisons may be misleading if model, workflow, and task-type differences dominate more than agent choice itself
- Alternatives considered:
  - stay Codex-only and optimize only the strongest existing telemetry path
  - support multi-agent tracking only through free-form notes instead of internal agent/provider detection
  - delay all generalization until a full Claude telemetry backend is available
- Current confidence:
  - `medium` that first-class agent labeling is worth doing now
  - `low-medium` that full Claude cost/usage ingestion will justify its implementation cost soon
- Evidence status:
  - supported by direct product demand to make the tool work for Claude too
  - supported by the fact that the domain model was already mostly agent-neutral and the first generalization step was cheap and low-risk
  - not yet validated by evidence that teams will actually maintain and analyze a meaningful amount of non-Codex history
- Evaluation plan:
  - measure whether non-Codex work starts appearing in real usage instead of remaining a purely hypothetical capability
  - compare whether the multi-agent extension produces better product decisions, such as clearer workflow recommendations or more useful cross-agent retrospectives
  - compare implementation and maintenance cost against actual new coverage gained:
    - how many goals are later attributable to non-Codex workflows through the universal contract
    - whether those goals produced decision-useful differences in quality, retries, or cost
    - whether lack of Claude auto-usage sync remained a blocker in practice
- Next re-evaluation trigger:
  - after a small but real sample of non-Codex goals exists
  - or when we decide whether to invest in a dedicated Claude telemetry backend
  - or after cross-agent analysis is attempted and we can judge whether the extra surface produced meaningful insight
- Notes:
  - `2026-04-02`: activated after the user explicitly requested making the tool work for Claude too, rather than only for Codex.
  - `2026-04-02`: the product constraint was tightened so the public CLI stays agent-agnostic; generalized instruction-file bootstrap support such as `CLAUDE.md` remains valid, while provider-specific telemetry should stay behind internal adapters.
  - `2026-04-02`: evaluation should treat this as a staged hypothesis, not a fully validated product direction, because the current benefit is broader tracking coverage while the main unresolved cost is a possible Claude telemetry backend.
  - `2026-04-04`: telemetry research completed — Claude Code stores all conversation events as JSONL files in `~/.claude/projects/{encoded-cwd}/{sessionId}.jsonl`, no SQLite. Token usage is available in every `assistant` event under `usage.input_tokens`, `usage.output_tokens`, `usage.cache_creation_input_tokens`, `usage.cache_read_input_tokens`. Model name is in every event. Subagent events are stored in `{sessionId}/subagents/` subdirectory. See detailed implementation plan below.

---

## Implementation Plan (drafted 2026-04-04)

### Background: what is already done

**Fully implemented:**
- `agent_name` and `model` fields in domain model, both validated
- `UsageBackend` protocol — pluggable backend design
- `ClaudeUsageBackend` class registered in `USAGE_BACKENDS["claude"]`
- Provider detection via `model_provider` in SQLite (`"anthropic"/"claude"`) for Codex-side threads
- Cost calculation framework: decimal arithmetic, `model_pricing.json` file-based pricing
- Cost audit, reporting, observability — already agent-agnostic in domain

**Partial / stub:**
- `ClaudeUsageBackend.resolve_window()` — class exists but delegates to `resolve_codex_usage_window()` internally (uses Codex OTEL log parsing, wrong for Claude)
- `find_thread_id()` queries SQLite — not applicable for Claude since Claude uses JSONL, not SQLite

**Not implemented:**
- Claude model prices in `model_pricing.json` — zero entries
- Claude-specific JSONL telemetry parsing
- CLI arguments for Claude paths (`--claude-root` or equivalent)
- Cost audit support for Claude goals (hardcoded to `codex_state_path / codex_logs_path`)
- Tests and fixtures for Claude usage window
- Claude model name aliasing (e.g. `claude-sonnet-4-6` → canonical form)

---

### Telemetry format (research result)

**Storage**: `~/.claude/projects/{encoded-cwd}/{sessionId}.jsonl`
where `{encoded-cwd}` = cwd path with `/` replaced by `-`.

**Assistant event structure (relevant fields):**
```json
{
  "type": "assistant",
  "sessionId": "uuid",
  "timestamp": "2026-04-04T10:23:00.000Z",
  "model": "claude-sonnet-4-6",
  "cwd": "/Users/viktor/...",
  "gitBranch": "claude/silly-noether",
  "usage": {
    "input_tokens": 1234,
    "output_tokens": 567,
    "cache_creation_input_tokens": 890,
    "cache_read_input_tokens": 234
  }
}
```

**Subagents**: same structure, stored in `{sessionId}/subagents/agent-{id}.jsonl`.

**Key difference from Codex**: no SQLite, no OTEL events, no `conversation.id` lookup.
Finding events by cwd is done by directory name, not by querying a database.

---

### Step 1 — Add Claude model pricing

**File**: `src/codex_metrics/data/model_pricing.json`

Add entries for current Claude models (as of 2026-04):
- `claude-opus-4-6` (claude-opus-4)
- `claude-sonnet-4-6` (claude-sonnet-4)
- `claude-haiku-4-5-20251001` (claude-haiku-4.5)
- Historical models if needed: `claude-3-5-sonnet`, `claude-3-opus`, `claude-3-haiku`

Price fields to add per model:
```json
{
  "input_per_million": <float>,
  "cached_input_per_million": <float>,
  "output_per_million": <float>
}
```

Add model alias resolution for Claude in `resolve_pricing_model_alias()`.

**Open decision**: which historical Claude models to include (depends on how far back user's history goes).

---

### Step 2 — Implement Claude JSONL telemetry parsing

**New function**: `_resolve_claude_usage_window_impl()` in `cli.py`

Algorithm:
1. Encode `cwd` to directory name: replace each `/` with `-` (leading slash → leading `-`)
2. Locate project dir: `claude_root / "projects" / encoded_cwd`
3. Collect JSONL files to parse:
   - All `*.jsonl` directly in the project dir (one per top-level session)
   - All `{sessionId}/subagents/agent-*.jsonl` recursively (subagent tokens — included per OD-1)
4. Parse each JSONL line: keep `type == "assistant"` events only
5. Filter by `started_at <= event.timestamp <= finished_at`
6. Sum across all matching events (all sessions + all subagents in window):
   - `usage.input_tokens` → `input_tokens`
   - `usage.output_tokens` → `output_tokens`
   - `usage.cache_creation_input_tokens` → `cached_input_tokens` (write cache, billed at higher rate)
   - `usage.cache_read_input_tokens` → summed into total but tracked separately if domain supports it
7. Detect model: take the model from the most recent assistant event in window
8. Calculate cost via `compute_event_cost_usd()` using pricing
9. Return 6-tuple: `(cost_usd, total_tokens, input_tokens, cached_input_tokens, output_tokens, model_name)`

**Scope boundary — goals vs sessions:**
- This function is for **goal-level cost only**: sum everything in `started_at..finished_at`, across all sessionIds and all subagents. One number per goal.
- Session-level analytics (per-session breakdown, session history) is a separate concern — each session stays its own entity and is not collapsed here. That layer is out of scope for this step.

**Notes**:
- `cache_creation_input_tokens` (writing to cache) is billed at ~1.25× input rate — must use correct pricing field
- `cache_read_input_tokens` (reading from cache) is billed at ~0.1× input rate — must use correct pricing field
- Both cache fields must be priced separately, not as plain input tokens

---

### Step 3 — Wire ClaudeUsageBackend to new implementation

Replace the stub `ClaudeUsageBackend.resolve_window()` body in `usage_backends.py`:
- Remove delegation to `resolve_codex_usage_window()`
- Remove `find_thread_id()` call (not needed — Claude lookup is by directory, not SQLite row)
- Call new `resolve_claude_usage_window(claude_root, cwd, started_at, finished_at, pricing_path)`

**Interface**: `UsageBackend.resolve_window()` currently takes `state_path` and `logs_path`.
Decision: reuse `state_path` as `claude_root` (e.g. `~/.claude`), ignore `logs_path` for Claude.
This avoids changing the protocol interface. Document the semantic reuse in a comment.

### Step 4 — Auto-detect backend at `codex-metrics start` (OD-2)

At goal start time, detect the active agent and record it as `agent_name` on the goal:
1. Check if `~/.claude/projects/{encoded-cwd}/` exists and contains JSONL files → `agent_name = "claude"`
2. Else check if Codex state path exists → `agent_name = "codex"`
3. Else leave `agent_name = None`

This means `agent_name` is written once at `start`, and `update` just reads it from the stored goal to pick the backend — no re-detection needed.

### Step 5 — Add `claude_root` to project config (OD-3)

**File**: `codex_metrics.json` (project-level config)

Add optional field `claude_root` (default: `~/.claude`).
No CLI flag needed — `update` reads it from config.
Codex analog: `codex_state_path` already exists in config.

### Step 6 — CLI: wire `update` to Claude backend

Ensure `codex-metrics update` routes to `ClaudeUsageBackend` when `agent_name == "claude"`:
- Read `claude_root` from project config
- Pass as `state_path` to `ClaudeUsageBackend.resolve_window()`
- Remove any Codex-only assumptions from the `update` flow

---

### Step 7 — Cost audit: Claude support (OD-5)

**File**: `src/codex_metrics/cost_audit.py`

Currently hardcoded to `codex_state_path`, `codex_logs_path`, `codex_thread_id`.
Refactor to route by `agent_name` on the goal:
- If `agent_name == "claude"`: use `claude_root` from config, call `ClaudeUsageBackend`
- If `agent_name == "codex"` or None: existing Codex path (unchanged)

Audit categories stay the same (`sync_gap`, `telemetry_unavailable`, etc.) — they are already agent-neutral concepts.

### Step 8 — Tests and fixtures

- Fixture: sample `~/.claude/projects/` directory structure with JSONL events (including subagents)
- Test `_resolve_claude_usage_window_impl()`:
  - time window filtering (events outside window excluded)
  - multi-session summing (two sessionId files, both counted)
  - subagent inclusion (agent-*.jsonl tokens summed)
  - cache token pricing (creation vs read rates applied correctly)
- Test model detection from events
- Test `ClaudeUsageBackend.resolve_window()` end-to-end
- Test auto-detection logic at `start`

### Step 9 — Model aliasing

Extend `resolve_pricing_model_alias()` for Claude variants:
- `claude-sonnet-4-6` → `claude-sonnet-4-6` (canonical, no change)
- `claude-3-5-sonnet-20241022` → `claude-3-5-sonnet` (strip date suffix)
- `claude-opus-4-6` → `claude-opus-4-6`
- Add lookup table for all known Claude release aliases

---

### Decisions (resolved 2026-04-04)

| # | Question | Decision |
|---|----------|----------|
| OD-1 | Include subagent JSONL tokens in goal cost? | **Yes** — subagents spend real tokens, must be included |
| OD-2 | Auto-detect Claude vs Codex? | **Auto** — detect by presence of `~/.claude/projects/{encoded-cwd}/` JSONL files; record `agent_name` explicitly on the goal at `start` time |
| OD-3 | `--claude-root` as CLI arg or config? | **Project config** — add `claude_root` field to `codex_metrics.json` (default `~/.claude`), no per-command flag needed |
| OD-4 | Multiple sessions in one goal window? | **Sum all** — if the goal spanned multiple Claude Code sessions (multiple `sessionId`s), sum token usage from all sessions whose events fall within `started_at..finished_at` |
| OD-5 | Cost audit for Claude in this iteration? | **Yes** — include full cost audit support for Claude goals |
| OD-6 | Historical Claude models in pricing.json? | **Current generation only** — opus-4-6, sonnet-4-6, haiku-4-5; no 3.x models needed |

---

## Full Transcript Pipeline for Claude Code (research 2026-04-09)

### Current status

The cost backend (Steps 1–9 above) is **complete**. `sync-usage` reads `~/.claude` telemetry and backfills cost/token data into the NDJSON ledger.

The **history pipeline** now supports all sources by default:
- `history-ingest` (default, `--source all`): reads both `~/.codex` and `~/.claude`; missing roots are skipped
- `history-ingest --source codex`: reads `~/.codex` only (JSONL rollout files + `state_5.sqlite` + `logs_1.sqlite`)
- `history-ingest --source claude`: reads `~/.claude/projects/` only — full session JSONL including subagents

Claude Code ingestion was implemented in `history_ingest.py` (see `_iter_claude_source_files`, `_import_claude_session_file`). Thread identity uses `sessionId` (one file = one thread). Normalize and derive stages are reused unchanged.

---

### Claude Code session format (verified 2026-04-09)

**Storage**: `~/.claude/projects/{encoded-cwd}/{sessionId}.jsonl`
`encoded-cwd` = cwd path with `/` replaced by `-` (leading `/` → leading `-`).

**Event types per file:**

| Type | Meaning |
|------|---------|
| `user` | User message with `message.content[]`, `sessionId`, `cwd`, `gitBranch`, `version`, `parentUuid` |
| `assistant` | Assistant turn with `message.content[]`, `message.usage`, `message.model`, `requestId`, `parentUuid` |
| `queue-operation` | Queue enqueue/dequeue for message dispatch — contains `content` (plain text of message) |
| `system` | Local command output, subtype `local_command`, has `content` |
| `last-prompt` | Last user prompt for the session (summary event) |

**Token usage** is inline in every `assistant` event under `message.usage`:
```json
{
  "input_tokens": 3,
  "cache_creation_input_tokens": 17704,
  "cache_read_input_tokens": 0,
  "output_tokens": 123
}
```

**No equivalent of `state_5.sqlite`** — all thread/session metadata is in inline event fields.

**Subagents**: `{sessionId}/subagents/agent-{id}.jsonl` — same format, same usage structure.

---

### Key differences from Codex pipeline

| Aspect | Codex | Claude Code |
|--------|-------|-------------|
| Thread metadata | `state_5.sqlite` → `threads` table | Inline in each event (`sessionId`, `cwd`, `version`) |
| Session file | `rollout-*.jsonl`, starts with `session_meta` event | `{uuid}.jsonl`, no single header event |
| Messages | `response_item` payload with `role` | Top-level `user` / `assistant` events |
| Token usage | `token_count` payload → `last_token_usage` dict | `assistant.message.usage` inline per turn |
| Thread identity | Explicit `thread_id` linking sessions | `sessionId` per file; no explicit retry/continuation grouping |
| Logs side-channel | `logs_1.sqlite` | None |
| Subagents | Not present | `{sessionId}/subagents/agent-*.jsonl` |

---

### What was built (implemented 2026-04-09)

**1. Ingest adapter** added to `history_ingest.py`:
- `_iter_claude_source_files(claude_root)` — scans `~/.claude/projects/{encoded-cwd}/*.jsonl` and `subagents/`
- `_import_claude_session_file()` — parses `user`/`assistant` events into `raw_messages` and `raw_token_usage`
- Thread metadata from inline event fields (no `state_5.sqlite`)
- Extended existing `history-ingest` command with `--source claude`; default later changed to `--source all`

**2. Thread identity**: chose simple — one file = one thread (sessionId). Subagent files share the parent's sessionId, so parent's `raw_threads` row is preserved via `INSERT OR IGNORE`.

**3. Normalize / Derive**: reused unchanged — operate on warehouse tables, not source format.

**4. Compare**: source-agnostic, no changes needed.

---

### Resolved decisions

| # | Question | Resolution |
|---|----------|------------|
| TD-1 | Thread identity: one file = one thread, or heuristic grouping? | Simple: one file = one thread (sessionId) |
| TD-2 | Extend existing command or add `ingest-claude-history`? | Extended `history-ingest` with `--source claude`; default later changed to `--source all` (both sources) |
| TD-3 | Subagent sessions: separate threads or merge into parent? | Merged into parent via shared sessionId + INSERT OR IGNORE |
| TD-4 | How far back to ingest? | All history; content-hash dedup prevents re-ingestion |

---

### Effort estimate

- **Simple path** (one file = one thread, no subagent merge, separate CLI command): ~1–2 days
- **Full path** (heuristic retry grouping, subagent merge, full test coverage): ~3–5 days
- Main bottleneck: thread identity design + test fixtures with realistic Claude Code JSONL samples
